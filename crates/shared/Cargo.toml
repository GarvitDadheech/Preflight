[package]
name = "preflight-shared"
version = "0.1.0"
edition = "2021"
description = "Common data types shared across the Preflight workspace"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
