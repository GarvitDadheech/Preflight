[package]
name = "preflight-report"
version = "0.1.0"
edition = "2021"
description = "Renders Preflight comparison results into report.md and report.json"

[dependencies]
preflight-shared = { path = "../shared" }
serde_json = "1"
